function [f] = TestNewSurfFitting(params,stimuli,response,surftype,errortype)

% Compute the Naka-Rushton function with negative binomial error. Sending
% in a vector of p's and r's to be able to set limits.

keyboard

