% This is a backwards compatibility wrapper and may be removed in the future.
% Please use nex2stro instead.
function stro = notnex2stro(filename)
if nargin < 1 || isempty(filename)
    stro = nex2stro();
else
    stro = nex2stro(filename);
end
